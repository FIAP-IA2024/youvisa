name: youvisa

services:
  mongo:
    image: mongo:7
    container_name: youvisa-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - youvisa-network

  minio:
    image: minio/minio:latest
    container_name: youvisa-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # Web console
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-minioadmin}
    volumes:
      - minio-data:/data
    networks:
      - youvisa-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio-init:
    # One-shot service that creates the bucket once MinIO is up.
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${S3_ACCESS_KEY:-minioadmin} ${S3_SECRET_KEY:-minioadmin} &&
      mc mb --ignore-existing local/${S3_BUCKET:-youvisa-files} &&
      echo 'bucket ready' && exit 0
      "
    networks:
      - youvisa-network

  api:
    build:
      context: ./app/api
      dockerfile: Dockerfile
    container_name: youvisa-api
    restart: unless-stopped
    ports:
      - "5555:5555"
    environment:
      - API_NODE_ENV=${API_NODE_ENV:-development}
      - API_HOST=${API_HOST:-0.0.0.0}
      - API_PORT=${API_PORT:-5555}
      - API_KEY=${API_KEY}
      - API_IS_DEBUG=${API_IS_DEBUG:-true}
      # Sprint 4 forces local Mongo regardless of host .env (which may still
      # point at Atlas for Sprint 3 compatibility on other branches).
      - MONGODB_URI=mongodb://mongo:27017
      - MONGODB_DATABASE=${MONGODB_DATABASE:-youvisa}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    depends_on:
      - mongo
    volumes:
      - ./app/api:/app
      - /app/node_modules
    networks:
      - youvisa-network
    command: npm run dev

  validation:
    build:
      context: ./app/validation
      dockerfile: Dockerfile
    container_name: youvisa-validation
    restart: unless-stopped
    ports:
      - "5556:5556"
    environment:
      - API_KEY=${API_KEY}
      - VALIDATION_PORT=5556
    networks:
      - youvisa-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5556/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  agent:
    build:
      context: ./app/agent
      dockerfile: Dockerfile
    container_name: youvisa-agent
    restart: unless-stopped
    ports:
      - "7777:7777"
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - AGENT_PORT=7777
      # Forced local for Sprint 4 (see api comment above).
      - MONGODB_URI=mongodb://mongo:27017
      - MONGODB_DATABASE=${MONGODB_DATABASE:-youvisa}
      - S3_ENDPOINT=${S3_ENDPOINT:-http://minio:9000}
      - S3_REGION=${S3_REGION:-us-east-1}
      - S3_BUCKET=${S3_BUCKET:-youvisa-files}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY:-minioadmin}
      - S3_SECRET_KEY=${S3_SECRET_KEY:-minioadmin}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - PORTAL_SECRET=${PORTAL_SECRET}
      - PORTAL_TTL_HOURS=${PORTAL_TTL_HOURS:-24}
      - PORTAL_BASE_URL=${PORTAL_BASE_URL:-http://localhost:3000}
      - API_URL=${API_URL:-http://api:5555}
      - API_KEY=${API_KEY}
      - CLAUDE_MODEL=${CLAUDE_MODEL:-claude-haiku-4-5}
      - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN:-}
      - VALIDATION_URL=${VALIDATION_URL:-http://validation:5556}
    depends_on:
      - mongo
      - minio
      - api
      - validation
    volumes:
      - claude_home:/home/node/.claude
      - ./app/agent/src:/app/src:cached
      - ./app/agent/tests:/app/tests:cached
    networks:
      - youvisa-network

  # NOTE: n8n is being phased out in Sprint 4. Kept here only until Phase 10
  # of the Sprint 4 plan, when its responsibilities are fully migrated to
  # the agent service. See: context/specs/2026-04-26-sprint-4-multi-agent/.
  n8n:
    image: n8nio/n8n:latest
    container_name: youvisa-n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - WEBHOOK_URL=${N8N_WEBHOOK_URL:-http://localhost:5678/}
      - API_URL=${API_URL:-http://api:5555}
      - API_KEY=${API_KEY}
    volumes:
      - ./docker/n8n:/home/node/.n8n
      - ./app/n8n/workflows:/workflows
    networks:
      - youvisa-network

networks:
  youvisa-network:
    driver: bridge

volumes:
  mongo-data:
  minio-data:
  claude_home:
    external: true
